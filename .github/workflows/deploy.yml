name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: "fa9c49d5-6ab6-4fa3-b57d-9fa901f08101"
          heroku_app_name: "seedy-fiuba-grupo-3" #Must be unique in Heroku
          heroku_email: "dbizari@fi.uba.ar"
          usedocker: true